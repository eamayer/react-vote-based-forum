import React from 'react';

function Header(){
  var mainStyle={
    textAlign: 'center'
  }
  return(
    <div style={mainStyle}>
      <h1>Welcome to the forum!</h1>
    </div>
  );
}

export default Header;
